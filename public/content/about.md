Lightpath calculates approximate flight trajectories between any two airports (aka 'great circle routes') to show how a flight traverses different light conditions, moving through daylight, twilight, and darkness.

The visualisation uses accurate astronomical calculations to model the sun's position and render precise twilight gradients. 